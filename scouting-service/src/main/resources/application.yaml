server:
  port: 8082

spring:
  application:
    name: scouting-service

  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://root:password@localhost:27017/scouting_db?authSource=admin}